<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:alertDialogTheme">@style/AppTheme.AlertDialog</item>
    </style>

    <!-- Explicit alert-dialog theme. Without this, the native <select>
         element's Android picker (a framework AlertDialog + ListView,
         confirmed via `uiautomator dump`: android:id/parentPanel /
         android:id/text1 CheckedTextView rows) rendered the launch splash
         drawable (@drawable/splash — the full-bleed dark-green logo image)
         behind its option rows, making the text unreadable. Swapping
         AppTheme.NoActionBar's own windowBackground to a diagnostic red
         had ZERO effect on the dialog, proving the dialog wasn't resolving
         windowBackground from the Activity's theme at all — it falls back
         to a default alertDialogTheme neither of our themes declared,
         which apparently chains back to the splash drawable. Declaring our
         own clean alertDialogTheme removes that ambiguity entirely. -->
    <style name="AppTheme.AlertDialog" parent="android:Theme.DeviceDefault.Dialog.Alert">
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- Post-splash theme Capacitor's BridgeActivity switches to.
         Brand dark green (#121a2a) is the WINDOW background AND the
         status/navigation bar colours so there is never a white
         strip: pre-Android-15 the bars are coloured directly; on
         Android 15 colours: window background #121a2a (shows behind
         the bars) + white glyphs, so there's never a white strip.

         windowOptOutEdgeToEdgeEnforcement=true: Android 15 (API 35)
         FORCES edge-to-edge for targetSdk 35, which makes the WebView
         draw under the status bar. The Android WebView reports
         env(safe-area-inset-top)=0, so the header rendered on top of
         the clock/battery ("header overlapping the notification
         bar"); StatusBar.setOverlaysWebView(false) is ignored under
         forced edge-to-edge (confirmed in logcat on an API-35
         emulator). This official opt-out restores normal insets so
         the OS reserves the status-bar strip and the header starts
         below it. Valid until we target SDK 36 / handle insets
         natively; harmless/ignored on pre-15. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">#121a2a</item>
        <item name="android:alertDialogTheme">@style/AppTheme.AlertDialog</item>
        <item name="android:statusBarColor">#121a2a</item>
        <item name="android:navigationBarColor">#121a2a</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>


    <!-- Launch/splash theme.

         Android 12+ (API 31+) SplashScreen API: the OS IGNORES
         android:windowBackground / android:background for the splash
         content and instead centres `windowSplashScreenAnimatedIcon`
         on `windowSplashScreenBackground`. With neither set it fell
         back to the FULL adaptive launcher icon (@mipmap/ic_launcher =
         its own ic_launcher_background circle + the brand foreground),
         which the OS then circle-masks — so the splash showed the
         wrong icon (a mismatched background disc, not the clean brand
         mark on navy). Pin the brand mark (foreground only, white on
         transparent) on the brand navy, and hand back to the app theme
         once the WebView is ready. These are the androidx
         core-splashscreen compat attrs (no android: prefix); they map
         to the platform attrs on API 31+ and are back-ported below.

         Pre-12: android:background=@drawable/splash still gives the
         branded full-window splash + dark bars, no white flash. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#121a2a</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@drawable/splash</item>
        <item name="android:statusBarColor">#121a2a</item>
        <item name="android:navigationBarColor">#121a2a</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
