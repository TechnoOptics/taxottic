<?xml version="1.0" encoding="utf-8"?>
<!-- Fully transparent icon for the Android 12+ SplashScreen API. The
     brand navy (windowSplashScreenBackground) fills the screen with no
     mark, so the launch splash is a clean solid colour instead of the
     mis-cropped adaptive-icon foreground that rendered as a "T with an
     underline". -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp" android:height="108dp"
    android:viewportWidth="108" android:viewportHeight="108">
    <path android:pathData="M0,0h108v108h-108z" android:fillColor="#00000000" />
</vector>
